# App
spring.application.name=country-explorer
server.port=8080

# MySQL Datasource
spring.datasource.url=jdbc:mysql://localhost:3306/m223_country_explorer?useSSL=false&serverTimezone=Europe/Zurich&allowPublicKeyRetrieval=true
spring.datasource.username=m223_user
spring.datasource.password=m223pass

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Run data.sql on startup (also for MySQL)
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
